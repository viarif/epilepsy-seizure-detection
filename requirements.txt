# Numerical and signal processing
numpy==2.2.3
scipy==1.15.2

# EEG/EDF loading
mne==1.9.0

# Thresholding and evaluation metrics
scikit-learn==1.6.1

# PyTorch is intentionally not pinned here because the local machine uses a
# CUDA build. Install a matching CPU/CUDA wheel separately. The current local
# training environment is C:\Users\ASUS\anaconda3\envs\unet.
